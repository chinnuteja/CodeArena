server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    client_max_body_size 2m;

    # Liveness for container / load balancer
    location /health {
        access_log off;
        return 200 'ok';
        add_header Content-Type text/plain;
    }

    # API — strip /api prefix (matches Vite dev proxy and REFRESH_COOKIE_PATH=/api/auth/refresh)
    location /api/ {
        proxy_pass http://api:4000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE verdict streams (/api/submissions/:id/stream)
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}

# Uncomment after placing TLS certs (e.g. certbot) and mounting deploy/certs:
#
# server {
#     listen 443 ssl http2;
#     server_name your-domain.example.com;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     root /usr/share/nginx/html;
#     index index.html;
#
#     location /health {
#         access_log off;
#         return 200 'ok';
#         add_header Content-Type text/plain;
#     }
#
#     location /api/ {
#         proxy_pass http://api:4000/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_buffering off;
#         proxy_cache off;
#         proxy_read_timeout 3600s;
#     }
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }
