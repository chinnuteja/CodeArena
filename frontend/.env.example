# Vercel project: https://code-arena-wine-iota.vercel.app/
# Set in Vercel → Settings → Environment Variables (Production)
# Point to your EC2 API origin — no trailing slash. Routes are /auth, /problems, /ai, etc.
VITE_API_URL=https://api.yourdomain.com
